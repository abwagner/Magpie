# Vendor reference materials — keep verbatim so future re-imports
# from the source bundle drop in cleanly.
docs/design_handoff_magpie/

# wasm-pack output (QF-133). Generated by `npm run build:wasm`; CI rebuilds
# and `git diff --exit-code`s the dir, so any prettier reformatting here
# would always diverge from CI's wasm-pack output and break the staleness check.
src/lib/wasm/

# Build output and dependencies.
dist/
node_modules/
# Anchored so /data/ at repo root is ignored but docs/data/*.md still gets
# formatted (gitignore-style `data/` without a leading slash matched any
# directory named "data" anywhere in the tree).
/data/
